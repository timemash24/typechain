{
  "include": ["src"],
  "compilerOptions": {
    "outDir": "build", // 컴파일된 파일 위치 설정
    "target": "ES6", // 컴파일 버전 설정
    "lib": ["ES6", "DOM"], // 어떤 API 사용하고 어떤 환경에서 코드실행하는지 설정
    // declaration file 불러옴
    "strict": true,
    "allowJs": true
  }
}
